type Conversation {
    id: ID!
    name: String
    creator: User
    lastMessage: Message
    image: String
    type: String
    participants: [User]
    updatedAt: String
}

type ConversationResult {
    totalPage: Int
    currentPage: Int
    conversations: [Conversation]
}