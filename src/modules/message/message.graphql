type Message {
    id: ID!
    conversation: Conversation
    users: User
    type: String
    message: String
    createdAt: String
}

type MessageResult {
    totalPage: Int,
    currentPage: Int,
    messages: [Message]
}