type User {
    id: ID!
    name: String!
    email: String!
    phone: String
    avatar: String
    birthday: String
    userActionId: Int
    status: Int
}

type Facebook {
    id: String
    name: String
}

type UserResult {
    users: [User]
    currentPage: Int
    totalPage: Int
}